/** @type {import('next').NextConfig} */
const nextConfig = {
  // ✅ Add your config options here
  reactStrictMode: true,
  images: {
    domains: [], // e.g. ['images.unsplash.com']
  },
};

module.exports = nextConfig;
